_target_: clients.client.iba_client_fn
_recursive_ : false
cfg:
  model: ${model}
  optimizers: ${optimizers}
  config_fit:
    lr: ${config_fit.lr}
  poisoned_batch_size: ${poisoned_batch_size}
  poisoned_target: ${poisoned_target}
  batch_size: ${batch_size}
  dataset:
    size: ${dataset.size}
